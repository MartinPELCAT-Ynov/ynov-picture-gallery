import { logger } from "server/utils/logger-utils";

logger.info("");

// import Router from "@koa/router";

// export const exampleController = new Router({
//   prefix: "/api/example",
// }).post("/start", () => {});
